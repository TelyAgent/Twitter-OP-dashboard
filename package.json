{
  "name": "op-dashboard",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Local-first ops dashboard. Static HTML + Supabase + DeepSeek AI + OpenCLI.",
  "scripts": {
    "start": "pkill -f 'node.*serve' 2>/dev/null; sleep 1; node src/serve.js",
    "db": "node scripts/db.js"
  }
}
